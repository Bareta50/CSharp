using System.Globalization;

namespace ErrorHandeling
{
    internal class Program
    {
        static void Main(string[] args)
        {
           /* List<int> numbers = new List<int> { 1, 2, 3, 4, 5 };    
            /* List<User> users = new List<User>

            {
                new User { Name = "John" },
                new User { Name = "Jane" },
                new User { Name = "Jack" }
            };
            string input = Console.ReadLine();
            // Defensive coding
            bool isValid = int.TryParse(input, out int number);
            if (!isValid)
            {
                Console.WriteLine("Not Valid Input");
            }

            int num = numbers.FirstOrDefault();
            User user = users.FirstOrDefault(x => x.Name == "Slave");
            if (user != null)
            {
                user.Name = "Slave";
            }
            
            string input1 = Console.ReadLine();

            try
            {
                int num1 = int.Parse(input1);
            }
            catch (ArgumentNullException ex)
            {
                Console.WriteLine("NULL is  passed as a value");
            }
            catch (FormatException ex) 
            {
                Console.WriteLine("Format of the string cannot be passed as a number");
            }
            catch (OverflowException ex)
            {
                string message = string.Format("Time: {0} Message: {2} StackTrace: {3}", DateTime.Now, ex.Message, ex.StackTrace);
                Console.WriteLine(ex.Message);
            }
            catch (Exception ex)
            {
                Console.WriteLine(ex.Message);
            }*/
        }
    }
}
